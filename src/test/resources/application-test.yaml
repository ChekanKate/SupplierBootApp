spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: "jdbc:h2:mem:supplyDBInMemory;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"
    username: "supplyuser"
    password: "supplypass"
    driver-class-name: org.h2.Driver
    initialization-mode: always
